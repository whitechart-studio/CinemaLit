#!/usr/bin/env python3
import sys
import os

# Ensure cinemalit package root is in sys.path
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from cinemalit.cli import main

if __name__ == "__main__":
    main()
