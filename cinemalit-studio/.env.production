# Google OAuth client ID is public (embedded in browser JS by design) — safe to commit.
# Vite auto-loads this file for `vite build` (mode=production), so it survives any
# build pipeline (Docker, Cloud Build, buildpacks) without needing a shipped .env file.
VITE_GOOGLE_CLIENT_ID=728509760223-6l8f6utu7rlpb5m2rimf71e3rhh1fjo7.apps.googleusercontent.com
