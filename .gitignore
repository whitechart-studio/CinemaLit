# Environment & Secret files
.env
.env.local
.env.*.local

# Python artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv

# CinemaLit runtime project state
.cinemalit/
test_workspace/

# ADK local dev runtime state (sessions, not code)
.adk/

# Local AI agent working rules & internal working memory (not shared)
agents.md
PRODUCT.md
ARCHITECTURE.md
DESIGN.md

# Locally installed agent skills & skill-manager state (not shared, machine-specific)
.agents/
.claude/
.impeccable/
skills-lock.json

# Node (root has no app of its own; stray root-level installs shouldn't happen,
# but keep this as a backstop)
node_modules/

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
.DS_Store
